// ont_paired.cpp - Paired-end mapping for ONT (not supported)
#include "ont_mapper.h"

namespace rnamapper {

// Note: ONT sequencing is single-molecule, so paired-end mode is not typical.
// The map_paired() implementation in ont_mapper.cpp displays an error message
// directing users to use map_single_end() for ONT data.

} // namespace rnamapper
